@using Microsoft.AspNetCore.Identity
<!DOCTYPE html>
@inject SignInManager<IdentityUser> SignInManager

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>@ViewBag.Title</title>
    <link href="~/Lib/Boostrap/css/bootstrap.css" rel="stylesheet" />
    <link href="~/Content/site.css" rel="stylesheet" />
    <link href="~/Content/style.css" rel="stylesheet">
</head>
<body>
    <div>
        <nav>
            <div class="logo">Komis</div>
            <div class="menu-area">
                <ul>
                    <li><a asp-controller="Home" asp-action="Index">Home</a></li>
                    <li><a asp-controller="Opinia" asp-action="Index">Opinie</a></li>
                    @if (SignInManager.IsSignedIn(User))
                    {
                        <form asp-area="" asp-controller="Account" asp-action="Logout" id="logoutForm"
                              method="post" class="contact-btn">
                            <li><a href="javascript:document.getElementById('logoutForm').submit()">Wyloguj</a></li>

                        </form>
                    }
                    else
                    {
                        <li><a asp-controller="Account" asp-action="Register">Rejestracja</a></li>
                        <li><a asp-controller="Account" asp-action="Login">Logowanie</a></li>
                    }
                </ul>


            </div>
            <div class="contact-btn"><button>co</button></div>
        </nav>
    </div>
    <div class="cointener body-content">
        <div class="row">
            @RenderBody()
        </div>
    </div>






    @*<nav class="navbar navbar-nav navbar-expand-lg  navbar-dark bg-primary">

            <a asp-controller="Home" asp-action="Index" class="navbar-brand">Home</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav mr-auto">
                    <li class="nav-item active">
                        <a asp-controller="Opinia" asp-action="Index" class="nav-link">Opinia</a>
                    </li>
              </ul>

                @if (SignInManager.IsSignedIn(User))
                {
                    <form asp-area="" asp-controller="Account" asp-action="Logout" id="logoutForm"
                          method="post" class="navbar-right">
                        <ul class="nav navbar-nav navbar-right">
                            <li><a href="javascript:document.getElementById('logoutForm').submit()">Wyloguj</a></li>
                        </ul>
                    </form>
                }
                else
                {
                    <ul class="collapse navbar-collapse">
                        <li><a id="registerLink" asp-controller="Account" asp-action="Register">Rejestracja</a></li>
                        <li><a id="loginLink" asp-controller="Account" asp-action="Login">Logowanie</a></li>
                    </ul>
                }

            </div>
        </nav>*@

</body>
</html>
